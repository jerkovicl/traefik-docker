# Traefik v3 static configuration

[log]
  level = "ERROR" # DEBUG, INFO, WARN, ERROR, FATAL, PANIC

# Preserve the previous backend TLS behavior from v1.
# This keeps Traefik from failing verification against self-signed or
# otherwise untrusted upstream HTTPS certificates.
[serversTransport]
  insecureSkipVerify = true

# Traefik dashboard
[api]
  dashboard = true

# Entry points
[entryPoints]
  [entryPoints.http]
    address = ":80"
    [entryPoints.http.http.redirections.entryPoint]
      to = "https"
      scheme = "https"
  [entryPoints.https]
    address = ":443"
    [entryPoints.https.http.tls]
      certResolver = "cloudflare"
      options = "default"
  [entryPoints.monitor]
    address = ":8081"

# Dynamic configuration provider (file-based)
[providers.file]
  watch = true
  filename = "/etc/traefik/rules.toml"

# Docker provider
[providers.docker]
  endpoint = "unix:///var/run/docker.sock"
  network = "traefik_proxy"
  watch = true
  # Only expose containers with traefik.enable=true label
  exposedByDefault = false

# Let's Encrypt / ACME via Cloudflare DNS challenge
[certificatesResolvers.cloudflare.acme]
  email = "email@domain.com" # any email id will work
  storage = "/etc/traefik/acme/acme.json"
  [certificatesResolvers.cloudflare.acme.dnsChallenge]
    provider = "cloudflare"
    delayBeforeCheck = 300
  [[certificatesResolvers.cloudflare.acme.domains]]
    main = "example.com"
    sans = ["*.example.com"]

